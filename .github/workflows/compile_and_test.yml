name: compile_and_test

on: 
  push:
jobs:
  Run_Tests:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code 
        uses: actions/checkout@v2
      - name: Setup CMake
        uses: jwlawson/actions-setup-cmake@v1.9
        with:
          cmake-version: '3.16.x'
      - name: Configure the build system for tests
        run: ./configure_tests.sh
      - name: Build Tests
        run: ./build.sh
      - name: Run Tests
        run: ./run.sh